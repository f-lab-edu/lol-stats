spring:
  config:
    activate:
      on-profile: beta

logging:
  level:
    ROOT: INFO
    com.github.lolstats: TRACE
    org.springframework.web.servlet: DEBUG
    org.springframework.web.filter: DEBUG
